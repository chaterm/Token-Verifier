# 安全策略

## 工具自身的安全设计

- API 密钥只从环境变量读，永不明文进配置文件或 rawData；manifest 只保留 8 位指纹
- rawData 默认 `digest` 级：不含模型输出原文，可安全公开
- **`full` 级（含原始输出文本）当前未实现，配置了会被拒绝**；一旦实现，也绝不进官方发布渠道

## 恶意端点威胁模型

`collect` / `run` 的威胁模型假设**被测端点本身可能是恶意的**（用户填入的
`base_url` 可能指向攻击者控制的主机）。在此假设下的防护：

- **不跟随 HTTP 重定向**：3xx 记为 `http_3xx` 错误。恶意端点可用重定向把
  认证头（尤其 `x-api-key` 这类自定义头，Go 默认策略不会剥离）转发到任意
  第三方主机
- **响应体大小封顶 512KiB**（非流式读取总量 / 流式内容累积总量），防
  gzip 炸弹与无限流耗尽内存；超限记为 `parse` 错误
- **`error_detail` 落盘前按 4KiB 截断**，防超大错误文案把证据文件单行撑过
  读回上限（16MB）导致文件永久不可读
- **verbose 报告剥控制字符**：分布取值源自服务端文本，渲染前过滤 C0 控制
  字符与 DEL，防 ANSI 转义序列伪造终端显示

这些防护不阻止凭证到达用户自己配置的端点（那是设计使然：`collect` 本来就要
带着密钥向 `base_url` 发请求）；它们阻止的是凭证被进一步扩散、以及恶意
响应伤害运行工具的机器。

## 报告漏洞

请**不要通过公开 issue 报告安全漏洞**。

通过 GitHub Security Advisories（仓库 Security 标签页 → Report a vulnerability）私下报告，或联系维护者。我们会在 72 小时内确认收到，修复流程走私下分支，披露时间与报告者协商确定。

## 报告范围

- 工具自身缺陷导致密钥/原始输出泄漏到 rawData 或日志
- digest 闸门可被绕过导致不可比数据被静默比较

不在范围内：被测端点本身的安全问题（请向对应厂商报告）、公开题库被针对性优化的对抗性问题（这是已知的设计权衡，见 docs/BASELINES.md 的说明）。
